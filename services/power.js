var Service = require("../core/service.js");

/**
 * 公告
 */
class Power extends Service {
  /**
   * 构造函数
   * @param {Object} config 配置参数
   */
  constructor(config) {
    // 传参给父类构造函数
    super();
  }
}

module.exports = {
  Power,
};
