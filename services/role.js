var Service = require('../core/service.js');

/**
 * 临时访问牌
 */
class Role extends Service {
  /**
   * 构造函数
   * @param {Object} config 配置参数
   */
  constructor(config) {
    // 传参给父类构造函数
    super();
  }
}

module.exports = {
  Role
};
